name: Release and propose dev promotion

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed

concurrency:
  group: release-main
  cancel-in-progress: false

jobs:
  release:
    name: TODO - publish immutable image and propose GitOps promotion
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      packages: write
      pull-requests: write
      attestations: write
      id-token: write
    steps:
      - name: Implement Milestones 3 and 4
        run: |
          echo "TODO: publish one image for the validated head_sha"
          echo "TODO: update dev desired state with the registry digest"
          echo "TODO: create an idempotent promotion pull request"
          exit 1
