name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: TODO - test, lint, render, and build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Implement Milestone 1
        run: |
          echo "TODO: implement the PR validation workflow from README.md"
          exit 1
